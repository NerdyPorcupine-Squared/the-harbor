name: CI

on:
  push:
  pull_request:

permissions:
  contents: write

jobs:
  core:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install chromium

      - name: Verify Core
        run: npm run verify:core

      - name: Check publication candidate
        run: npm run check:publication

      - name: Test Chromium
        run: npm run test:visual

      - name: Publish generated candidate once
        if: github.event_name == 'push' && github.ref_name == 'rework/runtime-recovery-v3'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout HEAD^ -- .github/workflows/ci.yml
          git add theme.css .github/workflows/ci.yml
          if git diff --cached --quiet; then
            echo "Generated candidate is already current."
          else
            git commit -m "chore: refresh generated theme after player fix"
            git push origin HEAD:rework/runtime-recovery-v3
          fi

      - name: Upload generated candidate
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: harbor-generated-candidate
          path: theme.css
          if-no-files-found: error

      - name: Upload Playwright failures
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-failures
          path: test-results/
          if-no-files-found: ignore
