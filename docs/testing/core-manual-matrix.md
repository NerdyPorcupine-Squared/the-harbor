# Harbor Core manual matrix

This file is a placeholder-only checklist for the repository owner. Do not add server addresses, account names, library names, media titles, screenshots, tokens, plugin inventories, logs, or machine paths.

Candidate commit: `COMMIT_SHA`

Result values: `Not run`, `Needs change`, or `Acceptable`.

## Desktop

- Home with Media Bar Enhanced — Result: `Not run`
- Parchment treasure-map browsing surfaces and framed full-color cards — Result: `Not run`
- Cinematic details-to-map transition — Result: `Not run`
- Media Bar cinematic-to-map transition — Result: `Not run`
- Header, drawer, tabs, and keyboard focus — Result: `Not run`
- Mixed library, filters, sorting, missing art, and progress — Result: `Not run`
- Search populated, empty, loading, and error states — Result: `Not run`
- Details, people, seasons, and episodes — Result: `Not run`
- Forms, menus, dialogs, dashboard, and tables — Result: `Not run`
- Player remains dark with no cartography over video — Result: `Not run`
- Player controls, seek, volume, captions, and fullscreen — Result: `Not run`
- Browser 200% zoom — Result: `Not run`
- Reduced motion — Result: `Not run`

## Mobile

- Home with Media Bar Enhanced — Result: `Not run`
- Reduced cartography density with no page overflow — Result: `Not run`
- Header and hero controls remain reachable — Result: `Not run`
- Library, search, and details have no page overflow — Result: `Not run`
- Touch targets are at least 40×40 CSS pixels — Result: `Not run`
- Login, dashboard, alerts, and errors — Result: `Not run`
- Player remains dark with no cartography over video — Result: `Not run`
- Player controls remain visible and clickable — Result: `Not run`
- Safe-area spacing in portrait and landscape — Result: `Not run`

## Optional absent-plugin check

The Media Bar-absent layout is covered by fixtures and Chromium. If the owner also tests Core with Media Bar Enhanced disabled, record only the sanitized result here — Result: `Not run`.

## Feedback template

- Matrix row:
- Result:
- Client type and viewport:
- Sanitized symptom:
- Keyboard or pointer steps:
- Expected visual behavior:
